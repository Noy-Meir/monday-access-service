# Backend API base URL.
# In development, leave this empty — the Vite proxy forwards /api to localhost:3000.
# In production, set this to the deployed backend URL (e.g. https://api.yourdomain.com).
VITE_API_URL=
