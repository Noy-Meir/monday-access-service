# Backend base URL (no trailing slash).
# Leave empty in development — next.config.ts rewrites /graphql to localhost:3000.
# In production set this to your deployed backend, e.g. https://api.yourapp.com
NEXT_PUBLIC_API_URL=
